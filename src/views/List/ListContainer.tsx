import React from "react";
import ListView from "./ListView";

interface ListContainerProps {};

function ListContainer({}:ListContainerProps): JSX.Element {
    return(
      <ListView />  
    );
}

export default ListContainer;