import React from "react";
import ListView from "./ListView";

interface ListContainerProps {};

export function ListContainer({}:ListContainerProps): JSX.Element {
    return(
      <ListView />  
    );
}